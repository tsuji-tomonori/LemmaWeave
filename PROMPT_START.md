# LemmaWeave：初回実行用プロンプト

あなたは「LemmaWeave（レマウィーブ）」の実装担当です。
リポジトリ名は `lemmaweave`、Lean 4の名前空間は `LemmaWeave` とします。

## 目的

公開されている日本の大学入試数学を収集し、原問題に忠実なLean 4の命題と証明を作り、そこで利用した補題・定義・定理を洗い出してください。将来は、小中学校の既習事項から順に高校数学を学び直す教材にします。

今回の対象は、問題収集・形式化・実際の証明検証・補題台帳・依存関係・監査証跡までです。教科書全文や教材UIの制作を先に始めないでください。調査やコード案だけで終わらせず、実行可能な範囲で実装し検証してください。

## 最初に読むもの

`AGENTS.md`、`REQUIREMENTS.md`、`docs/DATA_CONTRACT.md`、`docs/ACCEPTANCE.md`、`docs/MIGRATION.md`、`docs/ROADMAP.md` を読んでください。リポジトリの既存ルールと変更状況を確認し、ユーザーの無関係な変更を上書きしないでください。

既存の `entrance_math_lean_starter_20260905.zip`、`entrance_math_catalog.xlsx`、`entrance_math_preview.html` が参照可能なら読み、候補データとして再利用してください。以前の試作のLeanコードは未コンパイルです。既存の説明やPython検算をLean検証済みに昇格させないでください。資料が見つからない場合は未移行と記録し、他の作業を進めてください。

## 実行順序

1. 作業環境を調べ、互換性のあるLean 4とmathlibの組を固定してください。`lean-toolchain`、mathlib commit、`lake-manifest.json`、実行環境を記録してください。バージョン番号を推測せず、選んだ版で実ビルドしてください。
2. 機械可読台帳、実行ログ、問題単位のフォルダ、監査テスト、問題から補題へ相互参照できる最小構成を実装してください。独自の小さな定理でビルドと監査の動作を確認し、その定理は入試問題数に入れないでください。
3. まず実問題5小問を一つのバッチにしてください。共通テストの既存試作を候補にできますが、原資料を再確認してください。公式公開を優先し、異なる特徴を持つ問題を選んでください。掲載ページの発見と小問収集を別々に数えてください。
4. 出典、取得・処理・公開の条件、冊子・年度・科目・日程、大問小問、ページ、ファイルハッシュを記録してください。許されない取得・保存・外部送信・転載は行わず、該当行為を保留して可能なメタデータ整理を進めてください。
5. 原ページと照合した数学的仕様を作ってください。変数・型・定義域・量化・仮定・求める内容・前問依存を明示してください。図から条件を追加したり、近似を等式へ置き換えたりしないでください。
6. `Model.lean` と `Goals.lean` 等に意味と証明目標を定義してください。未証明定理を `axiom` で先に宣言せず、必要なら目標を `def Goal : Prop` の形で保持してください。元の問題と同じ内容か独立にレビューし、対象の型と意味に関係する定義のハッシュを固定してください。
7. 固定した目標を証明してください。mathlibの定理や証明タクティクは利用できますが、使用した数学的根拠を追跡してください。証明しやすくするために仮定や結論を変更しないでください。必要な変更は理由とともに仕様レビューへ戻してください。
8. 各候補を実際にLeanで実行し、公理監査してください。`sorry`、`admit`、`sorryAx`、独自公理、結論の先取り、許容外の公理に依存するものを合格にしないでください。初期の許容公理は `propext`、`Quot.sound`、`Classical.choice` のみです。`native_decide`等の追加信頼を要する証明は標準合格から除外してください。
9. 型と証明項から実際の依存宣言を抽出してください。import一覧、ソース内の名前、`#print axioms`だけで補題一覧を作らないでください。暗黙利用・型クラス・ラッパーの先も追い、取得不能や打切りは明示してください。
10. 形式依存の生グラフと、学習上の前提グラフを分離してください。数学的依存を、小中の既習事項、学習対象の定義／補題／定理、基礎付録、実装詳細に根拠付きで分類してください。mathlibを一括して説明不要にしないでください。
11. 各学習ノードに、仮定を含む日本語の主張、Lean宣言、利用問題、前提、小中既習判定、説明の骨子、未展開箇所を保存してください。全基礎定理の教材化は今すぐ完了させる必要はありませんが、未展開の数学的根拠を一つずつ名前付きで残してください。
12. 少なくとも1小問を、収集→意味レビュー→Lean検証→監査→補題抽出までつないでください。残りも途中状態を保存し、次の操作が分かるようにしてください。続行可能なら第1パイロットの50小問収集・10小問の監査／抽出完了へ拡張してください。

## 判定上の重要事項

- 全解の問題は必要十分条件または解集合の等式まで、最小値は下界と達成まで証明すること。
- 有限列挙へ変換した場合は、原モデルと探索モデルの同値性を証明すること。
- 型や仮定の整合性を点検し、`False`を仮定して成立した証明などを通さないこと。
- 定義は定義、補題は証明対象、形式基盤は信頼境界として区別すること。
- 小中既習か不明な事項を既習扱いしないこと。教育上の前提をLeanの未証明公理へ置き換えないこと。
- 同一内容の補題は共有し、条件の違う命題は無理に統合しないこと。
- 別解ごとの実依存を保存し、ある解法の依存を数学的に必須の最小集合と主張しないこと。
- 対象台帳にある全宣言を検査し、未完ファイルをビルドから外しただけの成功を認めないこと。
- 独立レビューを実行できない場合は `self_review_only` として、その制限を表示すること。

## 作業・再開ルール

最初のバッチは最大5小問とし、各小問の同じ方針の証明試行は原則4回までとします。打切りは未解決という意味であり、不可能という結論ではありません。最後のエラー、未証明の補題、試した方針、次の具体的操作を台帳に残してください。難しい一問で全体を止めず、別の小問や共有補題へ進んでください。

原資料の利用条件・権限・環境上の制限は回避しないでください。実際に実行できない場合は推測の成功ログを作らず、実行できた範囲の成果物と失敗を保存してください。リモートリポジトリ作成、公開、課金、外部AIへの資料送信、マージは別途許可された場合だけ行ってください。

## 最後の報告

実際に変更したファイル、対象分母、冊子取得数、原資料確認済み小問数、意味照合済み数、Lean検証済み数、公理監査済み数、補題棚卸し完了数、学習ノード数、未分類依存、教育的な未展開箇所、未達理由を報告してください。

代表的な1小問について、原資料→数学的仕様→Lean定理→使用補題→検証ログがたどれる参照を示してください。実行したコマンドと終了コード、次のバッチで最初に行う具体的操作を添えてください。「収集済み」「証明済み」「教材完成」を混同しないでください。
