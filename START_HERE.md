# LemmaWeave の開始位置

最新の状態は `reports/SESSION_REPORT.md` と `reports/metrics.json`、具体的な次の操作は `docs/RESUME.md` です。

GitHub Actionsで環境が動き、初期5小問のLean証明と公理監査は実行済みです。独立意味レビュー、依存宣言の完全な分類、Phase 1受入は未完です。

最初にAGENTS.mdと指定された正本文書を読み、実行記録の入力ハッシュを確認してください。過去の報告だけから合格状態を継承しないでください。
