# 実装状態

実装・実行済み：固定Lean/mathlib、9依存照合、ハッシュ付き実行ログ、問題・バリアント台帳、Model/Goalsの固定、初期5小問の独立意味レビュー・Lean証明・公理監査・実依存抽出。本文取得不能の自然なexported importを含む10ケース、定義元モジュール・相対ソースファイルの完全照合、取得できた宣言行範囲、全工程再実行とCI成果物の安全な取り込みも実装済み。

79件の日本語カードと182宣言の分類を保存。数学カードの追加44件は独立に確認し、構文木定義60件は閉じた型と列挙されたレビュー証拠に限定して検査する。学習上の前提は各証明バリアントが実際に使う宣言から生成する。31件のPython回帰テストが成功。

未完了：実際の原資料処理条件（AC01）、10,949宣言の分類・日本語説明と前提（AC09–11）、収集50小問/6分野・完了10小問/4分野のパイロット。現在は収集5小問/2分野・追加候補8小問・完全合格0小問。宣言行範囲が存在しない生成宣言等は境界を明記している。

`lw.py accept` は全受入条件とパイロット条件が満たされた場合だけ成功する。現状の未達条件を理由に終了コード2を返す。`acceptance_report.py` は各項目を現行の証拠で判定し、古い実行や名前だけの分類を合格に使わない。

最新実行と未完理由は `reports/SESSION_REPORT.md`、機械可読な判定は `reports/acceptance.json`、再実行証拠は `reports/latest-ci-replay.json` を参照。
